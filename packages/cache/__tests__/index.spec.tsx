import cache, {readCache, writeCache} from '..';

describe('cache', () => {
    it('', () => {
        expect(cache.get).toBeDefined();
        expect(cache.set).toBeDefined();
    })
});

describe('readCache', () => {
    it('', () => {
        expect(readCache).toBeDefined();
    })
});

describe('writeCache', () => {
    it('', () => {
        expect(writeCache).toBeDefined();
    })
});