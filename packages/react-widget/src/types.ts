export type manager = {
    getWidgets: Function,
    registerWidget: Function,
    register: Function,
};

export type globals = {
    React: any,
    ReactDOM: any,
};

export type window = any;
export type document = any;