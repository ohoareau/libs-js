import CrudService from '..';

describe('', () => {
    it('', async () => {
        const s = new CrudService('xxx');
        expect(s).toBeDefined();
        expect(s.getName()).toEqual('xxx');
    });
});
