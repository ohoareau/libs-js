import React from 'react';
import {Text} from '@react-pdf/renderer';

const DocumentCoverFrameBlock = () => {
    return (
        <Text>DocumentCoverFrameBlock</Text>
    );
};

export default DocumentCoverFrameBlock